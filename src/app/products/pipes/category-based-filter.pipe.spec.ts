import { CategoryBasedFilterPipe } from './category-based-filter.pipe';

describe('CategoryBasedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryBasedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
